version: 2

sources:
  - name: adzuna
    database: "{{ env_var('DBT_PROJECT_ID', 'datatalent-glaq-2') }}"
    schema: raw
    tables:
      - name: adzuna
        description: >
          Offres data engineer Adzuna FR.
          Source : api.adzuna.com, requête what=data+engineer, pays fr.
          WRITE_APPEND hebdomadaire, déduplication en staging.
