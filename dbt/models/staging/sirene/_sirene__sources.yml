version: 2

sources:
  - name: sirene
    database: "{{ env_var('DBT_PROJECT_ID', 'datatalent-glaq-2') }}"
    schema: raw
    tables:
      - name: sirene_etablissement
        description: >
          Stock brut INSEE des établissements chargé depuis le Parquet
          StockEtablissement. Source utilisée par le modèle
          stg_sirene__etablissements.
